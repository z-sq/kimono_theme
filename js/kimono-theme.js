/**
 * @file
 * Kimono Theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.kimonoTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
